//--------------------------------------------------------------------------------------------------

const Exec = require('app.exec');

//--------------------------------------------------------------------------------------------------

// Заливаем на GitHub
Exec([
    ['clear'],
    ['git', 'add', '.'],
    ['git', 'commit', '--amend', '--no-edit'],
    ['git', 'push', '-f']
], true);

//--------------------------------------------------------------------------------------------------